doctype html
html(lang="en")
  head
    title= Title
    link(rel="stylesheet" href=assetPath('main.css'))
    script.
      var gon = !{JSON.stringify(gon)};
  body
    #chat
    // block content
    script(src=assetPath('main.js'))
